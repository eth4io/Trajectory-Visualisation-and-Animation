import java.lang.Math.*;

static class Tools {
  /* returns the absolute eucledean distance between two Locations */
  /*
  static float eucDistance(Location l1, Location l2) {
    if (l1 != null && l2 != null) {
      return abs(sqrt(
                  pow(l1.getLat - l2.getLat, 2) + 
                  pow(l1.getLon - l2.getLon, 2)
                  ));
    } else
      return 0;
  }
  */
}
